<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:tf_behaviors="clr-namespace:Garage.RL.Behaviors.TextFields"
             x:Class="Garage.RL.Pages.Authentication.SignInPage">
  <Grid x:Name="signInPageGrid" Margin="10">
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <StackLayout Grid.Row="0">
      <Label Text="User authentication:" FontSize="Medium" HorizontalOptions="Start"/>
      <Entry x:Name="loginEntry" Placeholder="Login">
        <Entry.Behaviors>
          <tf_behaviors:ValueRequiredBehavior/>
        </Entry.Behaviors>
      </Entry>
      <Entry x:Name="passwordEntry" IsPassword="True" Placeholder="Password">
        <Entry.Behaviors>
          <tf_behaviors:ValueRequiredBehavior/>
        </Entry.Behaviors>
      </Entry>
    </StackLayout>
    <Grid Grid.Row="2">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="4*"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <StackLayout Grid.Column="1">
        <Button Text="Sign In" Clicked="SignInButtonClicked"/>
      </StackLayout>
    </Grid>
  </Grid>
</ContentPage>